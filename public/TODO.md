# TODO: Update Header Image to Vertical Oval

- [ ] Update `../src/components/Main.tsx`:
  - Import the new image: `import profileImg from '../assets/images/profile.png';`
  - Change the img src to `src={profileImg}`
- [ ] Update `../src/assets/styles/Main.scss`:
  - Change the img styles to `width: 150px; height: 200px; border-radius: 50%;` to make it a vertical oval.
- [ ] Run the app to verify the image displays correctly as a vertical oval.
